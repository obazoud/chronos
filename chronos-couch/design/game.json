{
  "_id": "_design/game",
  "updates": {
    "xml2json": "
      function (doc, req) {
        if(req.query.doc == null) {
          return [null, \"doc is null!\\n\"];
        }
        var xmlDoc = req.query.doc;
        var gamesession = new XML(xmlDoc);
        if (doc == null) {
          doc = {};
        }
        doc._id= 'gamejson';
        doc.type='gamejson';
        doc.title = gamesession.parameters.trackeduseridmail.text();
        return [doc, \"ok!\\n\"];
      }
    "
  }
}